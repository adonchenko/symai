# --- Build Stage ---
FROM golang:1.23-alpine AS builder
# Installing cmake, make, jdk
RUN apk update
RUN apk add --no-cache cmake make gcc g++ make musl-dev openjdk17-jre
# Set the working directory in the container
WORKDIR /app
# Copy the current directory contents into the container at /app
RUN mkdir /app/etc
RUN mkdir /app/etc/cmake
COPY ./CMakeLists.txt /app/CMakeLists.txt
COPY ./etc/cmake/FindGo.cmake /app/etc/cmake
RUN mkdir /app/bin
COPY ./bin /app/bin
RUN mkdir /app/src
RUN mkdir /app/src/go
COPY ./src/antlr4/BehaviorsGrammar.g4 /app/src/antlr4/BehaviorsGrammar.g4
COPY ./src/go/server/config/config.go /app/src/go/server/config/config.go
COPY ./src/go/server/config/symaiformatter.go /app/src/go/server/config/symaiformatter.go
COPY ./src/go/server/utils /app/src/go/server/utils
COPY ./src/go/server/frontend /app/src/go/server/frontend
COPY ./src/go/server/frontendcontroller/frontendservercontroller.go /app/src/go/server/frontendcontroller/frontendservercontroller.go
COPY ./src/go/server/frontendcontroller/frontendwebserver.go /app/src/go/server/frontendcontroller/frontendwebserver.go
# Build an executable
RUN mkdir /app/build
RUN cd /app/build
RUN cmake .
RUN make symai-symaicore-golang

# Stage 2: Create the final minimal image
FROM alpine:latest AS runner
#voumes
VOLUME /properties
VOLUME /logdir
VOLUME /tmpdir
#VOLUME /app/resources
WORKDIR /app
COPY --from=builder /app/build/release/symaicore /app/symaicore
EXPOSE 12345
CMD ["/app/symaicore"]
